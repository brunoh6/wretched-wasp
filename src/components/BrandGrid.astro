---
const { brands } = Astro.props;
---

<div class="grid grid-cols-2 sm:grid-cols-3 gap-6 items-center">
  {brands.map((brand) => (
    <img
      src={brand.logo}
      alt={brand.name}
      class="max-h-12 mx-auto opacity-80 hover:opacity-100 transition"
      loading="lazy"
    />
  ))}
</div>